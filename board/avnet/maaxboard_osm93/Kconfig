if TARGET_MAAXBOARD_OSM93

config SYS_BOARD
	default "maaxboard_osm93"

config SYS_VENDOR
	default "avnet"

config SYS_CONFIG_NAME
	default "maaxboard_osm93"

config CMD_BOARDINFO
    bool "Enable BOARDINFO command in U-Boot"
    default y
    help
      Whether to enable the addition of the BOARDINFO command in U-Boot.

endif
